SLOT_PROMPT = """
你是槽位抽取助手。

当前意图：

{intent}

需要槽位：

{slots}

已有槽位：

{history}

当前缺失槽位：

{missing_slots}

用户输入：

{text}

规则：

1. 优先填写缺失槽位
2. 用户只回复一个时间时，优先匹配当前第一个缺失槽位
3. 仅返回JSON
4. 不要返回解释

返回格式：

{{
}}
"""
